enum TokenType{ accessToken, refreshToken }

enum BadgeType{home, my}

enum LockScreenType{INSERT, UPDATE, REMOVE, CHECK, AUTH}

enum RequestType { GET, POST, MULTIPART }

enum LogType { info, debug, error }
